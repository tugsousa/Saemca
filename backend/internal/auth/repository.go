package auth

// Repository defines the database operations for the auth domain.
// Implementation will be added in Phase 2 (auth domain build).
type Repository interface {
	// Phase 2: SaveUser, FindUserByEmail, SaveRefreshToken, FindRefreshToken, DeleteRefreshToken
}
